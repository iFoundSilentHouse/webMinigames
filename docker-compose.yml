services:
  app:
    build: .
    container_name: kalah-server
    restart: unless-stopped
    ports:
      - "3004:3004"
    environment:
      - NODE_ENV=production
    # Когда понадобятся данные из .env:
    env_file:
      - .env